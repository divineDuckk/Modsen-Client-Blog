import { twMerge } from "tailwind-merge";

import { sen } from "@/app/fonts";

export const display = twMerge(`${sen.className} font-bold text-6xl`);
export const heading1 = twMerge(`${sen.className} font-bold text-5xl`);
export const heading2 = twMerge(`${sen.className} font-bold text-4xl`);
export const heading3 = twMerge(`${sen.className} font-bold text-3xl`);
export const heading4 = twMerge(`${sen.className} font-bold text-2xl`);
export const heading5 = twMerge(`${sen.className} font-bold text-xl`);
export const heading6 = twMerge(`${sen.className} font-bold text-base`);
